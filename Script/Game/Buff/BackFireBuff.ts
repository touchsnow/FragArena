import { _decorator, Component, Node } from 'cc';
import { BaseBuff } from './BaseBuff';
const { ccclass, property } = _decorator;

@ccclass('BackFireBuff')
export class BackFireBuff extends BaseBuff {
    
}
